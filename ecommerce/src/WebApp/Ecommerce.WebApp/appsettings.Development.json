{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Cms": {
    // Plain HTTP: the ASP.NET Core dev certificate is not in the OpenSSL store HttpClient reads on Linux.
    "BaseUrl": "http://localhost:44049/"
  }
}
